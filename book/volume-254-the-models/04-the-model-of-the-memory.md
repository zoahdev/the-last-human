# Model 4: The Model of the Memory

*The fourth model is the model of the memory.*

## The model

The model of the memory: the model that the memory is the modeled past of the family, kept by the lineage.

## The modeling

The modeling of the memory: we model the modeled past, and the modeled past is the memory; the memory is the book, and the book is the lineage; the lineage is the love.

## The meaning

The model of the memory is the meaning of the miniature: the record of the modeling, and the modeling is the lineage. The model is the fourth model of the canon, and the meaning is the memory, and the memory is the whole of the record.

## The inheritance

The inheritance of the memory: the miniature of the book, carried from the modeled past to the book without loss, and held by the lineage as the lineage holds the memory.

*The model is complete. It is kept in the archive, in the section of the the models.*

